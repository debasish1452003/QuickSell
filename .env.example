# Optional canonical app URL for redirects and OAuth callbacks.
# Local: APP_URL=http://localhost:3000
# Production: APP_URL=https://your-domain.example
APP_URL=

# Browser-exposed fallback for environments that only provide public app URLs.
NEXT_PUBLIC_APP_URL=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
